; Loaded via PHP_INI_SCAN_DIR (set by start.bat) so OPcache is enabled for the
; dev server WITHOUT touching the global php.ini. The global ini already carries
; opcache.* tuning values; it just never loads the extension itself.
zend_extension=opcache
opcache.enable=1
opcache.enable_cli=1
